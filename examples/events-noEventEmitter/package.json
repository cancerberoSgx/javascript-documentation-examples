{
  "name": "events-noEventEmitter",
  "version": "1.0.0",
  "description": "Can we document events that don't use node.js `EventEmitter`?, in other words, is there something special about the on() method and its first parameter from where the event name seems to be taken magically by typedoc??",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests here yet, it's fine\" && exit 0",
    "lint": "tslint src/**/*.ts",
    "lint-fix": "tslint --fix src/**/*.ts",
    "build": "tsc",
    "doc": "tsc && typedoc --options typedoc.ts; yarn run docco src/index.ts --output docs/docco",
    "doc-json": "tsc && typedoc --options typedoc.ts; yarn run docco src/index.ts --output docs/docco",
    "clean": "rm -rf node_modules build docs"
  },
  "keywords": [
    "typedoc",
    "typescript",
    "documentation",
    "jsdoc",
    "JavaScript"
  ],
  "author": "Sebastián Gurin",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^9.6.5",
    "docco": "^0.8.0",
    "ts-node": "^6.0.0",
    "tsconfig-paths": "^3.3.1",
    "tslint": "^5.9.1",
    "tslint-config-airbnb": "^5.8.0",
    "typedoc": "^0.11.1",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "typedoc": "^0.11.1",
    "typescript": "^2.8.3"
  }
}
